{
  "name": "dr-notif-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "dr-notif admin panel frontend (vanilla ES modules, no build step)",
  "type": "module",
  "scripts": {
    "test": "node --test \"js/**/*.test.js\""
  }
}
